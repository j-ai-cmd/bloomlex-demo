{
  "version": 2,
  "installCommand": "npm install",
  "buildCommand": "npm -w @spine/web run build",
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    { "source": "/v1/:path*", "destination": "/api" }
  ],
  "functions": {
    "api/index.ts": {
      "maxDuration": 60
    }
  }
}
