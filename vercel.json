{
  "version": 2,
  "installCommand": "npm install && cd apps/web && npm install",
  "buildCommand": "cd apps/web && npm run build && cp -r dist ../../public",
  "outputDirectory": "public",
  "rewrites": [
    { "source": "/v1/:path*", "destination": "/api" }
  ],
  "functions": {
    "api/index.ts": {
      "maxDuration": 60
    }
  }
}
